#!/bin/bash

adam_id="${1}"
adam_id="${adam_id//id/}"
country_code="ch"

if [ -z "${adam_id}" ]; then
  echo "Error: Please provide ADAM ID" && exit 1
fi

curl -s -o - "https://itunes.apple.com/lookup?id=${adam_id}&country=${country_code}" | jq -c
